int[] alternatingSums(int[] a) {
    int[] returnArray = new int[2] { 0, 0 };
    for (int i = 0; i < a.Length; i++) {
        if (i%2==0) {
            returnArray[0] += a[i];
        }
        else {
            returnArray[1] += a[i];
        }
    }
    return returnArray;
}
