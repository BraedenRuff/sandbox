int absoluteValuesSumMinimization(int[] a) {
    return a[(a.Length - 1) / 2];
}
//abs(a[0] - x) + abs(a[1] - x) + ... + abs(a[a.length - 1] - x)
//finds x for smallest possible sum
//array is sorted
